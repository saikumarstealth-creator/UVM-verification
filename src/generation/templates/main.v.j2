// Top-level UVM Testbench for {{ spec.design_name }} (standalone compilation entry)
// Auto-generated — wraps top_tb for iverilog/vcs/questa compatibility
`timescale 1ns/1ps
`include "uvm_macros.svh"
import uvm_pkg::*;

`include "testbench.sv"

module main;
  logic clk;
  logic rst_n;

  // Interface
  {{ spec.design_name }}_intf intf(clk, rst_n);

  // Protocol Core DUT
  {{ spec.design_name }}_core dut (
    .clk  (clk),
    .rst_n(rst_n),
    .wb_cyc    (intf.wb_cyc),
    .wb_stb    (intf.wb_stb),
    .wb_we     (intf.wb_we),
    .wb_addr   (intf.wb_addr),
    .wb_data_i (intf.wb_data_o),
    .wb_data_o (intf.wb_data_i),
    .wb_ack    (intf.wb_ack)
  );

  initial begin
    uvm_config_db#(virtual {{ spec.design_name }}_intf)::set(null, "*", "vif", intf);
    run_test("{{ spec.design_name }}_base_test");
  end

  initial begin clk = 0; forever #5 clk = ~clk; end
  initial begin
    rst_n = 0;
    repeat (10) @(posedge clk);
    rst_n = 1;
  end
  initial begin
    $dumpfile("sim.vcd");
    $dumpvars(0, main);
  end
endmodule
